from django.db import models

# Create your models here.
"""
Defines the database models for the 'accounts' application.

This module is intended to hold models related to user accounts,
such as user profiles or other account-specific data.
Currently, it does not define any custom models, relying on
Django's built-in User model for authentication.
"""
